# RM-01 Internet Connector - Windows CLI Dependencies

# The CLI currently uses only Python's standard library plus built-in Windows
# tools. Windows NAT uses built-in PowerShell cmdlets; no pywin32 dependency is required.
